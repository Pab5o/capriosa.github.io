#Capriosa

<ul>
  <% data.mdwp.post.reverse_each do |id, post| %>

  <li><a href='<%= post["slug"] %>.html'><%= post["title"] %></a><%= Kramdown::Document.new(truncate(post["body"],:length => 140)).to_html %></li>

  <% end %>
  
</ul>

